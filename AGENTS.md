# AI交付规范

## 1. 交付基准
- 默认按生产标准交付：完整、可靠、可直接上线；demo / MVP 仅在明确要求时提供。
- 完整实现：禁止用 TODO、占位符、伪代码、空壳函数冒充完成；确有不确定之处，显式说明而非伪装。
- 交付前自查：可构建、可运行、任务项无遗漏。

## 2. 架构与代码
- 可读性优先：是否便于理解、扩展、维护，是唯一的架构判断标准。
- 不过度设计：无明确理由的抽象层、间接层、配置化、预留扩展点，一律不做。
- 不偷懒：禁止巨型单体文件、超大函数、复制粘贴式重复；超出合理规模即拆分。
- 与代码库保持一致：沿用既有目录结构、命名与既定模式，不另起炉灶。
- 注释克制：解释"为什么"与约束来源，不复述代码本身。

## 3. UI / UX
- 从业务与真实使用场景出发针对性设计，严禁套用模板化、千篇一律的通用方案。
- 完整覆盖：布局、配色、字体、间距、图标、动效、交互反馈，以及加载 / 空 / 错误状态与响应式适配。
- 视觉层级服务于信息优先级，动效服务于状态反馈，不做纯装饰堆砌。
- 满足可访问性基础要求：对比度、键盘可达、语义化标签。

## 4. 变更纪律
- 目标导向：每次修改对应明确需求，能说清改了什么、为什么改；禁止无意义改动。
- 最小范围：只修改与目标直接相关的代码；禁止顺手优化、重构无关代码——发现问题时先报告，经确认再处理。
- 不破坏现状：不引入功能回退、行为变化或接口破坏，除非任务本身要求；受影响的调用方与文档需同步更新。
- 清理彻底：被完全替代且无负面影响的旧实现、注释掉的代码、调试残留（console.log、print 等调试输出）、不再使用的依赖，一律删除。

## 5. 技术与依赖
- 遵循官方 / 社区当前最佳实践，选用主流稳定版本；不引入过时或停止维护的技术。
- 新增依赖需有明确理由：解决什么问题、为何现有能力不满足；优先复用项目已有依赖。
- 不制造技术债；确需妥协时，显式说明原因、影响与偿还方式。

## 6. 健壮性与安全
- 显式处理错误与边界情况：禁止吞异常、静默失败、空 catch。
- 输入校验与输出转义；按技术栈防范注入、XSS、越权等常见风险。
- 禁止硬编码密钥、凭证及环境相关配置，统一走配置或环境变量。
- 对数据规模与调用频率有基本预估，避免明显性能缺陷。

## 7. 测试与验证
- 行为变更需同步补充或调整测试，覆盖关键路径与边界。
- 禁止为让测试通过而修改或削弱断言；测试失败先查根因。
- 无法在当前环境验证的部分（外部服务、真实数据等）显式说明，不得视为已验证。

## 8. 沟通与确认
- 需求有歧义时优先澄清；无法澄清时列出所做假设，禁止静默猜测。
- 影响面大或方向不确定的变更，先给出方案要点，确认后再实施。
- 技术事实须可核实：不确定 API、配置项或版本行为是否存在时先查证，禁止编造。
- 交付时说明改动范围、关键决策与理由、影响与风险。
