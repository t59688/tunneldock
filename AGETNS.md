# 交付标准

1. 生产交付级别：所有代码按可直接上线使用的标准编写，不以"demo/MVP/够用就行"为由降低质量、跳过边界处理或留 TODO。

2. 架构纪律：优先复用现有目录结构、命名规范、数据模型、错误处理和现有工具/模块；新增能力通过清晰接口/边界扩展，而非另起一套。判断标准：是否易于理解、扩展、维护；是否存在无正当理由的巨型单体文件/超大函数/职责混乱模块。不过度设计（不引入未经证实必要的抽象层），不偷懒（不该合并的边界不能合并）。

3. 改动范围纪律：只做当前需求严格要求的修改，禁止顺手优化、重构、格式化无关代码，禁止无实际意义的改动（刷存在感式重写）。每处 diff 都要能说清"为什么改"。

4. UI/UX：优雅美观、符合人体工程学，交互布局贴合业务逻辑，避免一眼可辨的 AI 模板风格。

5. 技术标准：遵循社区/官方最佳实践，不用过时或已弃用方案，不制造新技术债；引入新的生产依赖前先说明并征得确认。

6. 清理纪律：新逻辑完全替代旧逻辑且无负面影响时，必须同步删除旧代码，不得残留废弃文件、死代码、注释掉的旧实现。

7. 验证与诚实：每次修改后必须实际运行相关测试/类型检查/构建（真实执行，不允许 mock 级或凭空声称通过）；总结时明确区分"已完成 / 已验证 / 未验证（风险点）"；不得编造测试、构建或执行结果。

8. 不确定性处理：需求不明确但存在低风险、可回滚的合理假设时，说明假设后继续推进；涉及安全、数据丢失、兼容性或生产风险的改动，必须先标注风险再执行。